"$schema" = "schemas/schema.json"

[default]
description = "Allows reading sidecar status and tailing logs. Mutating commands (start/stop/restart) must be granted explicitly."
permissions = ["allow-status", "allow-logs"]
