{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sidecar:default",
    "sidecar:allow-start",
    "sidecar:allow-stop",
    "sidecar:allow-restart"
  ]
}
